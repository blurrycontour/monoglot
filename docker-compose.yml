# Production compose file: it pulls published images and needs nothing but a
# .env beside it. `docker compose up -d` on a bare host is the whole install —
# the API imports the dictionary and word forms itself on first start.
#
# On a checkout, docker-compose.override.yml is merged in automatically and
# builds from source instead. Delete or rename it to test the published images.
services:
  worker:
    image: ${IMAGE_REGISTRY:-ghcr.io/blurrycontour}/monoglot-worker:${IMAGE_TAG:-latest}
    restart: unless-stopped
    environment:
      WHISPER_MODEL: ${WHISPER_MODEL}
      WHISPER_DEVICE: ${WHISPER_DEVICE}
      WHISPER_COMPUTE_TYPE: ${WHISPER_COMPUTE_TYPE}
      WHISPER_BEAM_SIZE: ${WHISPER_BEAM_SIZE}
      WHISPER_IDLE_UNLOAD_SECONDS: ${WHISPER_IDLE_UNLOAD_SECONDS:-600}
    volumes:
      # The worker reads audio by path, so it must see the same files the API
      # wrote. Read-only: the worker never modifies content.
      - monoglot-data:/data:ro
      - models:/models
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:9000/health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  api:
    image: ${IMAGE_REGISTRY:-ghcr.io/blurrycontour}/monoglot-api:${IMAGE_TAG:-latest}
    restart: unless-stopped
    environment:
      DATABASE_PATH: /data/monoglot.db
      API_PORT: "8080"
      AUTH_TOKEN: ${AUTH_TOKEN}
      AUDIO_DIR: /data/audio
      RAW_DIR: /data/raw
      DATA_CACHE_DIR: /data/cache
      APK_PATH: /data/apk/monoglot.apk
      WORKER_URL: http://worker:9000
      INGEST_CRON_HOUR: ${INGEST_CRON_HOUR}
      INGEST_CRON_MINUTE: ${INGEST_CRON_MINUTE}
      INGEST_ON_START: ${INGEST_ON_START}
      TRANSLATE_FALLBACK: ${TRANSLATE_FALLBACK:-1}
      LLM_ENABLED: ${LLM_ENABLED}
      OLLAMA_URL: ${OLLAMA_URL}
      OLLAMA_MODEL: ${OLLAMA_MODEL}
      TZ: Europe/Stockholm
    volumes:
      - monoglot-data:/data
      # Per-container CPU/memory for the System screen. Read-only stops the
      # socket file being rewritten; it does NOT make the Engine API read-only,
      # so this is only acceptable because the API issues GETs and stays on the
      # LAN. A socket proxy would be the fix if either changed.
      - /var/run/docker.sock:/var/run/docker.sock:ro
    ports:
      # Bound to all interfaces so the phone on the LAN can reach it.
      # This service must not be exposed to the public internet.
      - "${API_PORT:-8080}:8080"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/api/health"]
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  models:
  # A named volume, not a bind mount: on a host without the repo there is no
  # ./data to bind, and compose would silently create a root-owned one.
  monoglot-data:
