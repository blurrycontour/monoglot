# faster-whisper runs CTranslate2, not PyTorch: a ~1GB image instead of ~4GB,
# and materially faster on CPU.
faster-whisper==1.1.1
# faster-whisper 1.1.1 imports requests but does not declare it as a
# dependency, so pin it here or the module fails to import at startup.
requests==2.33.0
fastapi==0.115.6
uvicorn[standard]==0.34.0
